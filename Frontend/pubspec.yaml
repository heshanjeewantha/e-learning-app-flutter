name: core_dashboard
description: "UEE PROJECT."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10+1
  go_router: ^14.1.1
  google_fonts: ^5.1.0
  uuid: ^3.0.6
  flutter_rating_bar: ^4.0.0
  provider: ^6.0.0
  fl_chart: ^0.69.0
  charts_flutter: ^0.11.0
  http: ^1.2.2 # Updated to the latest version in the 0.13.x series
  cupertino_icons: ^1.0.2
  modal_bottom_sheet: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/
    - assets/images/illustration/
    - assets/logo/
